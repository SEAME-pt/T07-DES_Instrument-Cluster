set(VEHICLE_SPEED_VERSION 1.0)
@PACKAGE_INIT@

# make sure we have all needed dependencies
include(CMakeFindDependencyMacro)
find_dependency(Threads REQUIRED)
find_dependency(nlohmann_json REQUIRED)

INCLUDE("${CMAKE_CURRENT_LIST_DIR}/Vehicle_speedApiTargets.cmake")
INCLUDE("${CMAKE_CURRENT_LIST_DIR}/Vehicle_speedCoreTargets.cmake")
INCLUDE("${CMAKE_CURRENT_LIST_DIR}/Vehicle_speedImplementationTargets.cmake")

check_required_components(
VehicleSpeed-api
VehicleSpeed-core



VehicleSpeed-implementation
)
